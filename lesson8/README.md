# Скрипти для сканування вразливостей

Це набір Python скриптів для роботи з вразливостями програмного забезпечення. Скрипти допомагають знайти встановлені програми на вашому комп'ютері та перевірити їх на наявність відомих вразливостей.

## 📋 Що таке вразливість?

**Вразливість** (vulnerability) - це помилка або слабке місце в програмі, яке може використати зловмисник для атаки на ваш комп'ютер.

**CVE** (Common Vulnerabilities and Exposures) - це унікальний номер для кожної вразливості, наприклад: CVE-2023-1234

**NVD** (National Vulnerability Database) - це велика база даних всіх відомих вразливостей у програмах. Це як енциклопедія проблем безпеки.

**CVSS** (Common Vulnerability Scoring System) - система оцінки вразливостей:
- **Score** (оцінка): число від 0 до 10 (10 = дуже небезпечно)
- **Severity** (рівень): CRITICAL (критичний), HIGH (високий), MEDIUM (середній), LOW (низький)

## 📁 Файли у проєкті

### 1. `get_installed_apps.py` - Пошук встановлених програм

**Що робить:**
- Автоматично визначає вашу операційну систему (Windows, macOS або Linux)
- Знаходить всі встановлені програми
- Виводить перші 20 програм на екран
- Зберігає повний список у файл `installed_apps.txt`

**Як запустити:**
```bash
python get_installed_apps.py
```

**Приклад виводу:**
```
Операційна система: Darwin 23.0.0
--------------------------------------------------
Знайдено програм: 142

  1. 1Password
  2. Adobe Acrobat Reader
  3. Firefox
  ...
```

**Як це працює (для новачків):**
1. Програма дізнається яка у вас ОС (Windows, Mac чи Linux)
2. Запускає спеціальну команду для цієї ОС:
   - **Windows**: читає реєстр (база даних Windows) через PowerShell
   - **macOS**: шукає файли .app у папці /Applications
   - **Linux**: використовує dpkg (Ubuntu) або rpm (Fedora)
3. Збирає всі назви програм у список
4. Зберігає список у текстовий файл

---

### 2. `nvd_basic.py` - Базовий пошук вразливостей

**Що робить:**
- Підключається до NVD через інтернет
- Шукає вразливості для однієї програми (OpenSSH у прикладі)
- Виводить перші 5 знайдених вразливостей з описами

**Як запустити:**
```bash
python nvd_basic.py
```

**Приклад виводу:**
```
Знайдено вразливостей: 347
--------------------------------------------------

CVE-2023-38408
  OpenSSH before 9.3p2 has a double free issue related to options.kex_algorithms...

CVE-2023-25136
  OpenSSH server (sshd) 9.1 introduced a double-free vulnerability...
```

**Як змінити програму для пошуку:**
Відкрийте файл і знайдіть рядок:
```python
data = search_nvd("OpenSSH", limit=5)
```
Замініть `"OpenSSH"` на будь-яку іншу програму, наприклад `"Firefox"` або `"Chrome"`.

**Як це працює:**
1. Програма надсилає HTTP запит (як браузер) до сервера NVD
2. Сервер шукає вразливості за назвою програми
3. Повертає результати у форматі JSON (текстовий формат даних)
4. Програма обробляє JSON і виводить інформацію красиво

---

### 3. `nvd_to_csv.py` - Пошук вразливостей із збереженням у CSV

**Що робить:**
- Шукає вразливості для програми (OpenSSH за замовчуванням)
- Збирає детальну інформацію: CVE ID, severity, score, опис, дату публікації
- Зберігає все у CSV файл (таблиця, яку можна відкрити в Excel)
- Показує статистику критичних вразливостей

**Як запустити:**
```bash
python nvd_to_csv.py
```

**Результат:**
- Файл `openssh_vulnerabilities.csv` з таблицею вразливостей
- Можна відкрити в Excel, Google Sheets або будь-якій програмі для таблиць

**Що таке CSV?**
CSV (Comma-Separated Values) - це текстовий файл з таблицею, де:
- Кожен рядок - це рядок таблиці
- Значення розділені комами
- Перший рядок містить назви колонок

**Приклад CSV файлу:**
```csv
CVE ID,Severity,Score,Description,Published,Link
CVE-2023-38408,HIGH,7.5,OpenSSH before 9.3p2...,2023-07-20,https://nvd.nist.gov/vuln/detail/CVE-2023-38408
```

---

### 4. `homework_vulnerability_scanner.py` - Сканер для ваших програм

**Що робить:**
- Перевіряє список ВАШИХ програм на вразливості
- Для кожної програми шукає інформацію в NVD
- Зберігає результати у `my_vulnerabilities.csv`
- Показує статистику: скільки CRITICAL та HIGH вразливостей

**Як використовувати:**

**Крок 1:** Відкрийте файл і знайдіть:
```python
MY_APPS = [
    "Firefox",
    "Chrome",
    "VLC",
    "7-Zip",
    "Notepad++"
]
```

**Крок 2:** Замініть на свої програми (можна взяти з `installed_apps.txt`)

**Крок 3:** Запустіть:
```bash
python homework_vulnerability_scanner.py
```

**ВАЖЛИВО про Rate Limit:**
NVD API має обмеження: **5 запитів на 30 секунд** без API ключа.

Що це означає:
- Можна зробити тільки 5 запитів за 30 секунд
- Якщо робити більше - сервер заблокує вас
- Тому програма чекає 6 секунд між запитами

Скільки часу це займе:
- 5 програм = ~30 секунд (5 × 6 секунд)
- 10 програм = ~60 секунд (10 × 6 секунд)
- 20 програм = ~120 секунд (20 × 6 секунд)

**Приклад виводу:**
```
============================================================
СКАНЕР ВРАЗЛИВОСТЕЙ
============================================================

[1/5] Пошук вразливостей для 'Firefox'...
  Знайдено: 3 вразливостей
    - CVE-2023-5217: HIGH (7.5)
    - CVE-2023-4863: CRITICAL (8.8)
    - CVE-2023-4573: HIGH (7.5)
  Очікування 6 секунд (rate limit)...

[2/5] Пошук вразливостей для 'Chrome'...
...

============================================================
Збережено 15 записів у my_vulnerabilities.csv

Підсумок:
  Всього вразливостей: 15
  CRITICAL: 2
  HIGH: 8
```

---

### 5. `nvd_pandas_analysis.py` - Розширений аналіз з pandas

**Що робить:**
- Завантажує багато вразливостей (50 штук)
- Робить статистичний аналіз за допомогою pandas
- Показує мінімум, максимум, середнє значення CVSS score
- Фільтрує тільки критичні та високі вразливості
- Сортує за небезпечністю
- Зберігає у CSV

**Що таке pandas?**
Pandas - це бібліотека Python для роботи з таблицями даних (як Excel, але в коді).
**DataFrame** - це таблиця в pandas з рядками і колонками.

**Як запустити:**
```bash
python nvd_pandas_analysis.py
```

**Що нового порівняно з іншими скриптами:**
- Статистичний аналіз (середнє, мінімум, максимум)
- Фільтрація даних (тільки CRITICAL та HIGH)
- Сортування за оцінкою
- Підрахунок розподілу за severity
- Топ-5 найнебезпечніших вразливостей

**Приклад виводу:**
```
============================================================
СТАТИСТИКА ВРАЗЛИВОСТЕЙ
============================================================

Загальна кількість: 50

Розподіл за рівнем критичності:
  HIGH: 23
  MEDIUM: 18
  CRITICAL: 6
  LOW: 3

CVSS Score:
  Мінімальний: 3.3
  Максимальний: 9.8
  Середній: 6.85

Топ-5 найкритичніших вразливостей:
  CVE-2023-1234: 9.8 (CRITICAL)
  CVE-2023-5678: 9.1 (CRITICAL)
  ...

Високий ризик (CRITICAL + HIGH): 29

Дані збережено у nginx_vulnerabilities.csv
```

---

## 🚀 Швидкий старт для початківців

### Крок 1: Переконайтеся що Python встановлений

Відкрийте термінал (командний рядок) і введіть:
```bash
python --version
```

Повинно показати щось типу: `Python 3.11.0`

Якщо Python не встановлений:
- **Windows/Mac**: завантажте з [python.org](https://www.python.org/downloads/)
- **Linux**: `sudo apt install python3` (Ubuntu) або `sudo dnf install python3` (Fedora)

### Крок 2: Встановіть необхідні бібліотеки

```bash
pip install requests pandas
```

**Що це робить:**
- `requests` - для роботи з інтернетом (HTTP запити)
- `pandas` - для роботи з таблицями даних

### Крок 3: Знайдіть свої програми

```bash
python get_installed_apps.py
```

Відкрийте файл `installed_apps.txt` і подивіться список програм.

### Крок 4: Перевірте програми на вразливості

1. Відкрийте `homework_vulnerability_scanner.py`
2. Знайдіть список `MY_APPS`
3. Замініть на 5-10 програм з вашого `installed_apps.txt`
4. Запустіть:
```bash
python homework_vulnerability_scanner.py
```

### Крок 5: Подивіться результати

Відкрийте файл `my_vulnerabilities.csv` в Excel або Google Sheets.

---

## 📖 Основні концепції Python (для тих хто бачить код вперше)

### Що таке змінна?

Змінна - це "коробка" для зберігання даних з назвою:
```python
name = "Firefox"      # Текст (string)
count = 42           # Число (integer)
score = 7.5          # Число з комою (float)
is_critical = True   # Істина/Хибність (boolean)
```

### Що таке список?

Список - це колекція елементів у квадратних дужках:
```python
apps = ["Firefox", "Chrome", "VLC"]

# Доступ до елементів:
apps[0]  # "Firefox" (перший елемент, нумерація з 0)
apps[1]  # "Chrome" (другий елемент)
apps[-1] # "VLC" (останній елемент)

# Довжина списку:
len(apps)  # 3
```

### Що таке словник?

Словник - це пари "ключ: значення" у фігурних дужках:
```python
vulnerability = {
    "cve_id": "CVE-2023-1234",
    "severity": "HIGH",
    "score": 7.5
}

# Доступ до значень:
vulnerability["cve_id"]    # "CVE-2023-1234"
vulnerability["severity"]  # "HIGH"
```

### Що таке функція?

Функція - це блок коду з назвою, який можна викликати багато разів:
```python
def hello(name):
    print(f"Привіт, {name}!")

hello("Артем")  # Виведе: Привіт, Артем!
hello("Марія")  # Виведе: Привіт, Марія!
```

### Що таке цикл?

Цикл - це повторення дій:
```python
# Цикл for - проходить по кожному елементу
for app in ["Firefox", "Chrome", "VLC"]:
    print(app)
# Виведе:
# Firefox
# Chrome
# VLC

# Цикл з enumerate - дає і індекс, і елемент
for i, app in enumerate(["Firefox", "Chrome"]):
    print(f"{i+1}. {app}")
# Виведе:
# 1. Firefox
# 2. Chrome
```

### Що таке умова?

Умова - це виконання коду тільки якщо щось правда:
```python
score = 7.5

if score > 7.0:
    print("Високий ризик!")
elif score > 4.0:
    print("Середній ризик")
else:
    print("Низький ризик")
```

### Що таке list comprehension?

Це короткий спосіб створити новий список:
```python
# Звичайний спосіб:
critical = []
for v in vulnerabilities:
    if v["severity"] == "CRITICAL":
        critical.append(v)

# List comprehension (те саме, але коротше):
critical = [v for v in vulnerabilities if v["severity"] == "CRITICAL"]
```

### Що таке import?

Import - це підключення готових інструментів (модулів):
```python
import requests  # Підключаємо модуль requests
import csv       # Підключаємо модуль csv

# Тепер можемо використовувати функції з цих модулів:
response = requests.get("https://example.com")
```

---

## 🔧 Як працює HTTP запит до API?

API (Application Programming Interface) - це спосіб спілкування програм між собою.

**Аналогія з рестораном:**
- Ви (клієнт) = ваша програма
- Офіціант (API) = NVD API
- Кухня (сервер) = база даних NVD

**Як це працює:**
1. Ви робите запит (як замовлення в меню): "Дайте мені вразливості для Firefox"
2. API передає запит на сервер (офіціант йде на кухню)
3. Сервер обробляє запит і шукає дані (кухня готує)
4. API повертає результат (офіціант приносить їжу)

**В коді це виглядає так:**
```python
url = "https://services.nvd.nist.gov/rest/json/cves/2.0"
params = {"keywordSearch": "Firefox"}

response = requests.get(url, params=params)
data = response.json()  # Перетворюємо відповідь у словник Python
```

---

## ⚠️ Важливі обмеження

### Rate Limit NVD API

**Без API ключа:**
- 5 запитів на 30 секунд
- Максимум 10 запитів на хвилину

**З API ключем (безкоштовний):**
- 50 запитів на 30 секунд
- Набагато швидше!

**Як отримати API ключ:**
1. Зареєструйтеся на [nvd.nist.gov](https://nvd.nist.gov/developers/request-an-api-key)
2. Отримаєте ключ на email
3. Додайте у код:
```python
headers = {
    "apiKey": "ваш-ключ-тут",
    "User-Agent": "Mozilla/5.0"
}
```

---

## 🎯 Практичні завдання для навчання

### Завдання 1: Модифікуйте get_installed_apps.py
Змініть програму щоб вона виводила не 20, а 10 програм:
- Знайдіть рядок `for i, app in enumerate(apps[:20], 1):`
- Замініть `[:20]` на `[:10]`

### Завдання 2: Змініть програму для пошуку
У `nvd_basic.py` замініть "OpenSSH" на іншу програму:
- Спробуйте "nginx", "apache", "mysql"
- Подивіться скільки вразливостей знайдено

### Завдання 3: Збільште limit
У `nvd_to_csv.py` змініть кількість результатів:
- Знайдіть `vulnerabilities = fetch_vulnerabilities(keyword, limit=20)`
- Замініть `limit=20` на `limit=50`

### Завдання 4: Додайте свої програми
У `homework_vulnerability_scanner.py`:
- Додайте 3-5 програм з вашого комп'ютера до списку `MY_APPS`
- Запустіть сканування
- Подивіться які вразливості знайдено

### Завдання 5: Змініть фільтр
У `nvd_pandas_analysis.py` змініть фільтр вразливостей:
- Знайдіть `high_risk = df[df["severity"].isin(["CRITICAL", "HIGH"])]`
- Залиште тільки `["CRITICAL"]`
- Подивіться скільки критичних вразливостей

---

## 📚 Корисні ресурси для навчання

### Python для початківців:
- [Python.org Tutorial](https://docs.python.org/3/tutorial/) - офіційний підручник
- [Learn Python](https://www.learnpython.org/) - інтерактивні уроки
- [Codecademy Python](https://www.codecademy.com/learn/learn-python-3) - покрокове навчання

### Про API та HTTP:
- [What is an API?](https://www.youtube.com/watch?v=s7wmiS2mSXY) - відео пояснення
- [HTTP Request Methods](https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods) - GET, POST і т.д.

### Pandas:
- [Pandas Tutorial](https://pandas.pydata.org/docs/user_guide/10min.html) - 10 хвилин про pandas
- [Pandas Cheat Sheet](https://pandas.pydata.org/Pandas_Cheat_Sheet.pdf) - шпаргалка

### Про кібербезпеку:
- [NVD Website](https://nvd.nist.gov/) - офіційний сайт бази вразливостей
- [CVE Details](https://www.cvedetails.com/) - пошук CVE з деталями
- [CVSS Calculator](https://www.first.org/cvss/calculator/3.1) - розрахунок CVSS score

---

## ❓ Часті питання (FAQ)

### Q: Що робити якщо програма видає помилку "ModuleNotFoundError: No module named 'requests'"?
**A:** Встановіть бібліотеку: `pip install requests`

### Q: Чому програма так довго працює?
**A:** Через rate limit NVD API. Між запитами обов'язкова затримка 6 секунд.

### Q: Як прискорити роботу?
**A:** Отримайте безкоштовний API ключ на nvd.nist.gov - це дає 50 запитів на 30 секунд замість 5.

### Q: Чому для деяких програм не знайдено вразливостей?
**A:** Можливі причини:
- Програма дуже нова або мало відома
- У NVD використовується інша назва (наприклад, "Google Chrome" замість "Chrome")
- Дійсно немає відомих вразливостей

### Q: Що означає "N/A" у результатах?
**A:** "Not Available" - інформація відсутня (немає даних про severity або score).

### Q: CSV файл не відкривається правильно в Excel?
**A:** Спробуйте:
1. Відкрити Excel
2. Файл → Відкрити
3. Вибрати "All Files" і вибрати ваш CSV
4. Вказати кодування UTF-8

### Q: Можна використовувати ці скрипти комерційно?
**A:** Так, але зверніть увагу:
- NVD API безкоштовний навіть для комерційного використання
- Дотримуйтесь rate limits
- Дані NVD у публічному доступі

---

## 🛠️ Поширені помилки та їх вирішення

### Помилка: "HTTP Error 403: Forbidden"
**Причина:** Сервер заблокував запит (можливо перевищено rate limit)
**Рішення:** Почекайте 30 секунд і спробуйте знову

### Помилка: "Timeout"
**Причина:** Сервер не відповів вчасно
**Рішення:** Перевірте інтернет-з'єднання і спробуйте знову

### Помилка: "KeyError: 'vulnerabilities'"
**Причина:** Неочікуваний формат відповіді від API
**Рішення:** Перевірте чи працює NVD API на їхньому сайті

### Помилка: "UnicodeDecodeError"
**Причина:** Проблеми з кодуванням тексту
**Рішення:** Переконайтеся що використовується `encoding="utf-8"`

---

## 🎓 Що далі?

Після вивчення цих скриптів ви можете:

1. **Вдосконалити функціональність:**
   - Додати GUI (графічний інтерфейс) з tkinter
   - Зробити веб-версію з Flask
   - Додати автоматичну періодичну перевірку

2. **Розширити аналіз:**
   - Додати графіки з matplotlib
   - Інтегрувати з іншими базами вразливостей
   - Створити звіти у PDF

3. **Автоматизація:**
   - Налаштувати автоматичний запуск (cron на Linux, Task Scheduler на Windows)
   - Надсилати email з критичними вразливостями
   - Інтегрувати з системою моніторингу

4. **Вивчити більше:**
   - REST API дизайн
   - Асинхронне програмування (async/await)
   - Машинне навчання для аналізу вразливостей

---

## 📄 Ліцензія

Ці скрипти створені для навчальних цілей. Використовуйте вільно, змінюйте, вдосконалюйте!

---

## 🤝 Підтримка

Якщо у вас виникли питання:
1. Перечитайте коментарі у коді - там детальні пояснення
2. Погугліть помилку - багато відповідей на Stack Overflow
3. Запитайте у ChatGPT або Claude - AI помічники чудово пояснюють код

---

**Успіхів у навчанні! 🚀**

Пам'ятайте: кожен програміст колись був початківцем. Найважливіше - практикувати і не боятись помилок!
