# 📝 Домашнє завдання: Сканер вразливостей

## 🎯 Мета завдання

Створити власний сканер вразливостей, який буде перевіряти встановлені на вашому комп'ютері програми на наявність відомих вразливостей через NVD API.

---

## 📋 Завдання

Вам потрібно **завершити реалізацію** файлу `homework_vulnerability_scanner.py`.

Зараз у файлі є тільки структура з коментарями `# TODO:`. Ваше завдання — написати код замість цих коментарів.

---

## 🔍 Що потрібно зробити

### Крок 1: Дослідити приклади

Перед початком роботи **обов'язково** вивчіть ці файли:
- `nvd_basic.py` — як робити запити до NVD API
- `nvd_to_csv.py` — як обробляти відповідь API і зберігати у CSV

Зверніть увагу на:
- Як формується URL та параметри запиту
- Як витягується інформація з JSON відповіді
- Як створюється CSV файл

### Крок 2: Реалізувати функцію `search_vulnerabilities()`

Ця функція повинна:

1. **Виконати HTTP запит до NVD API** з використанням `requests.get()`
   - URL: `https://services.nvd.nist.gov/rest/json/cves/2.0`
   - Параметри: `keywordSearch` (назва програми), `resultsPerPage` (кількість результатів)
   - Headers: встановити User-Agent

2. **Обробити відповідь** у форматі JSON
   - Пройтися по списку `vulnerabilities`
   - Для кожної вразливості витягти потрібні поля

3. **Повернути список словників**, де кожен словник містить:
   - `"App"` — назва програми (параметр `app_name`)
   - `"CVE ID"` — ідентифікатор вразливості
   - `"Severity"` — рівень небезпеки (CRITICAL, HIGH, MEDIUM, LOW)
   - `"Score"` — CVSS оцінка (0-10)
   - `"Description"` — опис вразливості
   - `"Published"` — дата публікації

**Підказка:** Подивіться як це зроблено у `nvd_to_csv.py` у функціях `fetch_vulnerabilities()` та `get_cvss_info()`.

### Крок 3: Реалізувати функцію `main()`

Ця функція повинна:

1. **Пройтися циклом** по всіх програмах у списку `MY_APPS`

2. Для кожної програми:
   - Вивести повідомлення типу "Пошук вразливостей для Firefox..."
   - Викликати `search_vulnerabilities(app, limit=3)`
   - Додати результати до загального списку `all_results`
   - **ВАЖЛИВО:** Додати затримку `time.sleep(6)` між запитами (обмеження NVD API)

3. **Зберегти результати** у CSV файл `my_vulnerabilities.csv`
   - Використайте модуль `csv`
   - Створіть `csv.DictWriter` з полями: App, CVE ID, Severity, Score, Description, Published
   - Запишіть заголовки та всі рядки

4. **Показати статистику:**
   - Загальна кількість знайдених вразливостей
   - Кількість CRITICAL вразливостей
   - Кількість HIGH вразливостей

### Крок 4: Налаштувати список програм

1. Запустіть `get_installed_apps.py` щоб отримати список ваших програм
2. Відкрийте файл `installed_apps.txt`
3. Виберіть 5-10 програм зі списку
4. Замініть список `MY_APPS` у файлі `homework_vulnerability_scanner.py` на ваші програми

**Примітка:** Не всі програми будуть знайдені в NVD. Якщо програма не знайдена — це нормально.

---

## ⚙️ Технічні вимоги

### Обов'язкові елементи коду:

✅ Використовувати `requests.get()` для HTTP запитів
✅ Обробляти JSON відповідь через `.json()`
✅ Витягувати CVSS інформацію (score та severity)
✅ Додавати `time.sleep(6)` між запитами
✅ Зберігати результати у CSV файл
✅ Виводити статистику (загальна кількість, CRITICAL, HIGH)

### Обробка помилок:

```python
try:
    # Ваш код тут
except requests.exceptions.RequestException as err:
    print(f"Помилка при запиті до API: {err}")
```

### Rate Limit NVD API:

⚠️ **КРИТИЧНО ВАЖЛИВО!**

NVD API має обмеження: **5 запитів на 30 секунд** (без API ключа).

Що це означає:
- Між кожним запитом **обов'язково** робити паузу `time.sleep(6)` (6 секунд)
- Якщо не додати затримку — API заблокує вас на деякий час
- Для 5 програм сканування займе ~30 секунд
- Для 10 програм — ~60 секунд

---

## 📤 Що здавати

### 1. Файл `homework_vulnerability_scanner.py`
Повністю реалізований код з:
- Функцією `search_vulnerabilities()`
- Функцією `main()`
- Вашим списком програм у `MY_APPS`

### 2. Файл `my_vulnerabilities.csv`
Результат роботи вашого скрипта з знайденими вразливостями

### 3. (Опціонально) Скріншот виводу програми
Покажіть як працює ваш сканер у консолі

---

## 📊 Приклад очікуваного виводу

```
============================================================
СКАНЕР ВРАЗЛИВОСТЕЙ
============================================================

[1/5] Пошук вразливостей для 'Firefox'...
  Знайдено: 3 вразливостей
  Очікування 6 секунд (rate limit)...

[2/5] Пошук вразливостей для 'Chrome'...
  Знайдено: 3 вразливостей
  Очікування 6 секунд (rate limit)...

[3/5] Пошук вразливостей для 'VLC'...
  Знайдено: 3 вразливостей
  Очікування 6 секунд (rate limit)...

[4/5] Пошук вразливостей для '7-Zip'...
  Знайдено: 2 вразливостей
  Очікування 6 секунд (rate limit)...

[5/5] Пошук вразливостей для 'Notepad++'...
  Знайдено: 3 вразливостей

============================================================
РЕЗУЛЬТАТИ СКАНУВАННЯ
============================================================

Збережено 14 записів у my_vulnerabilities.csv

Статистика:
  Всього вразливостей: 14
  CRITICAL: 1
  HIGH: 7
  MEDIUM: 5
  LOW: 1

============================================================
```

---

## 📊 Приклад CSV файлу

Ваш `my_vulnerabilities.csv` повинен виглядати приблизно так:

```csv
App,CVE ID,Severity,Score,Description,Published
Firefox,CVE-2023-5217,HIGH,7.5,Heap buffer overflow in vp8 encoding...,2023-09-27
Firefox,CVE-2023-4863,CRITICAL,8.8,Heap buffer overflow in WebP...,2023-09-25
Chrome,CVE-2023-4572,HIGH,7.4,Use after free in MediaStream...,2023-08-29
...
```

---

## 💡 Підказки

### Як витягти CVE ID?
```python
cve_id = cve.get("id", "N/A")
```

### Як витягти опис англійською?
```python
descriptions = cve.get("descriptions", [])
description = next(
    (d["value"] for d in descriptions if d.get("lang") == "en"),
    "No description available"
)
```

### Як отримати CVSS score та severity?
Подивіться функцію `get_cvss_info()` у `nvd_to_csv.py` — там є повний приклад.

### Як зберегти у CSV?
```python
import csv

fieldnames = ["App", "CVE ID", "Severity", "Score", "Description", "Published"]

with open("my_vulnerabilities.csv", "w", newline="", encoding="utf-8") as f:
    writer = csv.DictWriter(f, fieldnames=fieldnames)
    writer.writeheader()
    writer.writerows(all_results)
```

---

## ⏱️ Скільки часу займе?

- **Вивчення прикладів** (nvd_basic.py, nvd_to_csv.py): 20-30 хвилин
- **Написання коду** функції `search_vulnerabilities()`: 30-40 хвилин
- **Написання коду** функції `main()`: 20-30 хвилин
- **Налаштування та тестування**: 10-15 хвилин
- **Запуск сканування** (для 5 програм): ~30 секунд

**Загалом: 1.5-2 години** (для тих хто вперше працює з Python та API)

---

## ❓ Часті помилки

### 1. Забули додати затримку
```python
# ❌ НЕПРАВИЛЬНО
for app in MY_APPS:
    results = search_vulnerabilities(app)
    all_results.extend(results)

# ✅ ПРАВИЛЬНО
for app in MY_APPS:
    results = search_vulnerabilities(app)
    all_results.extend(results)
    time.sleep(6)  # Обов'язкова затримка!
```

### 2. Неправильно обробляють JSON
```python
# ❌ НЕПРАВИЛЬНО
cve = item["cve"]  # Може викликати KeyError

# ✅ ПРАВИЛЬНО
cve = item.get("cve", {})  # Поверне {} якщо немає ключа
```

### 3. Не перевіряють наявність metrics
```python
# ❌ НЕПРАВИЛЬНО
score = metrics["cvssMetricV31"][0]["cvssData"]["baseScore"]

# ✅ ПРАВИЛЬНО
if "cvssMetricV31" in metrics and metrics["cvssMetricV31"]:
    score = metrics["cvssMetricV31"][0].get("cvssData", {}).get("baseScore")
```

---

## 🎓 Критерії оцінювання

| Критерій | Бали |
|----------|------|
| Функція `search_vulnerabilities()` працює правильно | 30% |
| Функція `main()` реалізована повністю | 30% |
| Додано затримку `time.sleep(6)` | 10% |
| Правильно зберігається CSV файл | 15% |
| Виводиться статистика | 10% |
| Код читабельний та прокоментований | 5% |

**Додаткові бали:**
- +5% за обробку помилок (try/except)
- +5% за красивий вивід у консоль
- +10% за додаткову функціональність (графіки, детальний звіт, тощо)

---

## 🆘 Що робити якщо застрягли?

1. **Перечитайте приклади коду** у `nvd_to_csv.py` — там є все необхідне
2. **Погугліть помилку** — багато відповідей на Stack Overflow
3. **Запитайте ChatGPT або Claude** — опишіть що намагаєтеся зробити
4. **Напишіть викладачу** — краще запитати ніж здати неправильно

---

## 📚 Корисні посилання

- [NVD API Documentation](https://nvd.nist.gov/developers/vulnerabilities)
- [Python requests library](https://requests.readthedocs.io/)
- [Python csv module](https://docs.python.org/3/library/csv.html)
- [JSON работа в Python](https://docs.python.org/3/library/json.html)

---

## ✅ Чеклист перед здачею

- [ ] Код запускається без помилок
- [ ] Функція `search_vulnerabilities()` повертає список словників
- [ ] Між запитами є затримка 6 секунд
- [ ] Створюється файл `my_vulnerabilities.csv`
- [ ] CSV файл можна відкрити в Excel
- [ ] Виводиться статистика (всього, CRITICAL, HIGH)
- [ ] Список `MY_APPS` містить ваші програми (не дефолтні)
- [ ] Код має коментарі що пояснюють логіку

---

**Успіхів у виконанні! 🚀**

Пам'ятайте: це практичне завдання, яке допоможе вам зрозуміти:
- Як працювати з REST API
- Як обробляти JSON дані
- Як зберігати результати у CSV
- Як працює аналіз вразливостей у реальному світі
